<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mny.wan.pkg">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application>
        <activity android:name=".presentation.main.WanMainActivity"></activity>
        <activity
            android:name=".presentation.setting.ThemeSettingsActivity"
            android:label="@string/title_activity_theme_settings" />
        <activity android:name=".presentation.coin.CoinDetailActivity" />
        <activity android:name=".presentation.mine.MineActivity" />
        <activity android:name=".presentation.LauncherActivity" />
        <activity android:name=".presentation.main.system.secondsystem.SystemChildrenActivity" />
        <activity android:name=".presentation.webview.WebViewActivity" />
        <activity android:name=".presentation.search.SearchActivity" />
        <activity android:name=".presentation.collect.CollectActivity" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".presentation.login.LoginActivity" />

        <meta-data
            android:name="com.mny.wan.pkg.app.WanConfig"
            android:value="ModuleConfig" />
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" /> <!-- 适配华为（huawei）刘海屏 -->
        <meta-data
            android:name="android.notch_support"
            android:value="true" /> <!-- 适配小米（xiaomi）刘海屏 -->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />
    </application>

</manifest>